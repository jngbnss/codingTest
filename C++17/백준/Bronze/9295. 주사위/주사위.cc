#include<bits/stdc++.h>
using namespace std;
int n,m,d,a,b;
string s;
vector<int>v;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(0);cout.tie(0);
	cin>>n;
	for(int i=1;i<=n;i++){
		cin>>a>>b;
		cout<<"Case "<<i<<": "<<a+b<<'\n';
	}
		
}