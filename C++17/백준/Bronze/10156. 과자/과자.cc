#include<bits/stdc++.h>
using namespace std;
long long n,a,b,c,sum,mn=1e9,ret=-50;
vector<int>v;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(0);cout.tie(0);
	cin>>a>>b>>c;
	if(a*b-c>0) cout<<a*b-c;
	else cout<<0;
		
	
}