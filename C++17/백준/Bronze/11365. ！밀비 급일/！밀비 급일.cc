#include<bits/stdc++.h>
using namespace std;
int n,x,y,ret,sum,l,a,b,c,d;
string s;
vector<int>v,v1;

int main(){
	while(true){
		getline(cin,s);
		if(s=="END") return 0;
		reverse(s.begin(),s.end());
		cout<<s<<'\n';
	}
		
}