#include<bits/stdc++.h>
using namespace std;
int n,a,b,ret,x,temp;
long long  A,B,C;
char c;
int arr[]={1,1,2,2,2,8};

string s;
vector<int>v;
int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(0);cout.tie(0);
	cin>>a;
	cin>>s;
	for(auto i:s){
		ret+=i-'0';
	}cout<<ret;
		
}