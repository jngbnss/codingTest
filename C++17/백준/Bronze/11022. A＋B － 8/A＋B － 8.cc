#include<bits/stdc++.h>
using namespace std;
int n,ret,a,b,idx,mn=1e9,mx=-1e9,temp,cnt;
string s;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(0);cout.tie(0);
	cin>>n;
	for(int i=1;i<=n;i++){
		cin>>a>>b;
		cout<<"Case #"<<i<<": "<<a<<" + "<<b<<" = "<<a+b<<'\n';
	}
}