#include<bits/stdc++.h>
using namespace std;
int n,a,b,ret,x,temp;
long long  A,B,C;
char c;

string s;
vector<int>v;
int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(0);cout.tie(0);
	cin>>s;
	cout<<s.size();
}